import React, { useState } from 'react';
import { createRoot } from 'react-dom/client';
import { AlertCircle, Check, ChevronLeft, Headphones, MapPin, Package, RefreshCw, Truck } from 'lucide-react';
import './index.css';

const steps = [
  { label: 'Processing', description: 'Your order is being prepared', date: 'May 24', icon: Package },
  { label: 'Shipped', description: 'Package picked up by courier', date: 'May 25', icon: Truck },
  { label: 'Out for Delivery', description: 'Your package is on the way', date: 'Today', icon: MapPin },
  { label: 'Delivered', description: 'Arrives at your doorstep', date: 'Estimated May 27', icon: Check },
];

function App() {
  const [view, setView] = useState('ready');
  const [expanded, setExpanded] = useState(false);
  if (view === 'loading') return <main className="flex min-h-screen items-center justify-center px-5"><div className="w-full max-w-md rounded-3xl border border-slate-200 bg-white p-8 text-center shadow-card"><RefreshCw className="mx-auto animate-spin text-brand" size={28} /><h1 className="mt-4 text-lg font-bold">Loading your order</h1><p className="mt-2 text-sm text-slate-500">Fetching the latest tracking update…</p></div></main>;
  if (view === 'error') return <main className="flex min-h-screen items-center justify-center px-5"><div className="w-full max-w-md rounded-3xl border border-slate-200 bg-white p-8 text-center shadow-card"><div className="mx-auto flex h-12 w-12 items-center justify-center rounded-full bg-rose-50 text-rose-600"><AlertCircle /></div><h1 className="mt-4 text-lg font-bold">We couldn’t load this order</h1><p className="mt-2 text-sm leading-6 text-slate-500">Please check your connection and try again. Your order is safe.</p><button className="primary mt-6 w-full" onClick={() => setView('ready')}>Try again <RefreshCw size={16} /></button></div></main>;
  return <main className="min-h-screen bg-[#f7f8fc] px-4 py-5 sm:px-6"><div className="mx-auto max-w-md"><header className="flex items-center justify-between"><button className="icon-button" aria-label="Go back"><ChevronLeft size={20} /></button><h1 className="text-sm font-bold text-ink">Order tracking</h1><button className="icon-button" onClick={() => setView('error')} aria-label="Show error state"><AlertCircle size={18} /></button></header><section className="mt-6"><p className="text-sm text-slate-500">Arriving by</p><div className="mt-1 flex items-end justify-between gap-3"><div><h2 className="text-3xl font-extrabold tracking-tight text-ink">Monday, May 27</h2><p className="mt-1 text-sm text-slate-500">Between 9:00 AM – 6:00 PM</p></div><span className="rounded-full bg-amber-50 px-3 py-1.5 text-xs font-bold text-amber-700">In transit</span></div></section><section className="mt-7 rounded-3xl border border-slate-200 bg-white p-5 shadow-card"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-wider text-slate-400">Current status</p><h2 className="mt-1 text-xl font-bold text-ink">Out for delivery</h2></div><div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-indigo-50 text-brand"><Truck size={23} /></div></div><div className="mt-7 space-y-0">{steps.map((step, index) => { const Icon = step.icon; const active = index === 2; const completed = index < 2; return <div className="relative flex gap-4" key={step.label}><div className="relative flex w-8 shrink-0 justify-center"><div className={`z-10 flex h-8 w-8 items-center justify-center rounded-full ${active ? 'bg-brand text-white ring-4 ring-indigo-100' : completed ? 'bg-emerald-500 text-white' : 'bg-slate-100 text-slate-400'}`}>{completed ? <Check size={15} /> : <Icon size={15} />}</div>{index < steps.length - 1 && <span className={`absolute top-8 h-12 w-px ${completed ? 'bg-emerald-300' : 'bg-slate-200'}`} />}</div><div className="min-h-[72px] flex-1 pb-5"><div className="flex items-start justify-between gap-3"><div><p className={`text-sm font-bold ${active ? 'text-brand' : 'text-slate-800'}`}>{step.label}</p><p className="mt-1 text-xs text-slate-500">{step.description}</p></div><p className="text-xs font-medium text-slate-400">{step.date}</p></div></div></div> })}</div></section><section className="mt-4 rounded-3xl border border-slate-200 bg-white p-5 shadow-card"><p className="text-xs font-semibold uppercase tracking-wider text-slate-400">Your order</p><div className="mt-4 flex gap-4"><div className="flex h-20 w-20 shrink-0 items-center justify-center rounded-2xl bg-slate-100 text-slate-400"><Package size={30} /></div><div className="min-w-0"><h2 className="font-bold text-ink">Wireless headphones</h2><p className="mt-1 text-sm text-slate-500">Midnight black · Qty 1</p><p className="mt-2 text-sm font-semibold text-slate-800">$129.00</p></div></div>{expanded && <div className="mt-4 border-t border-slate-100 pt-4 text-sm leading-6 text-slate-500">Delivering to <span className="font-semibold text-slate-700">18 Park View Road, Dhaka 1212</span>. Tracking updates may take a few minutes to appear.</div>}<button className="mt-4 text-sm font-semibold text-brand" onClick={() => setExpanded(!expanded)}>{expanded ? 'Hide details' : 'View delivery details'}</button></section><button className="primary mt-4 w-full rounded-2xl py-3.5"><Headphones size={17} /> Contact support</button><p className="mt-5 text-center text-xs text-slate-400">Order #VS-48291 · Last updated just now</p></div></main>;
}
createRoot(document.getElementById('root')).render(<App />);
