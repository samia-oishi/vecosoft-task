import React, { useState } from 'react';
import { createRoot } from 'react-dom/client';
import { Check, CircleAlert, Lightbulb, Send } from 'lucide-react';
import './index.css';

const prompts = ['Explain the data structure(s) you used and why you selected them.', 'Explain the time and space complexity of your implementation.', 'Describe one realistic situation or access pattern where your implementation could perform poorly, or where your approach has a limitation.', 'If you helped solve the problem, identify one suggestion from the AI that you changed, rejected, or improved, and explain why.'];
function App() { const [answer, setAnswer] = useState(''); const [error, setError] = useState(''); const [submitted, setSubmitted] = useState(false); function submit(event) { event.preventDefault(); if (answer.trim().length < 80) { setError('Please provide a more detailed explanation (at least 80 characters).'); setSubmitted(false); return; } setError(''); setSubmitted(true); } return <main className="min-h-screen px-4 py-8 sm:px-6 lg:py-12"><div className="mx-auto max-w-3xl"><header className="mb-7 flex items-center justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-indigo-600">Task 03</p><h1 className="mt-1 text-2xl font-bold tracking-tight text-ink sm:text-3xl">Algorithm explanation</h1><p className="mt-1 text-sm text-slate-500">Show the thinking behind your LRU cache implementation.</p></div><span className="rounded-full bg-indigo-50 px-3 py-1.5 text-xs font-semibold text-indigo-700">Critical thinking</span></header><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-card sm:p-7"><div className="flex items-start gap-4 rounded-xl border border-indigo-100 bg-indigo-50/60 p-4"><div className="rounded-lg bg-white p-2 text-indigo-600 shadow-sm"><Lightbulb size={19} /></div><div><p className="text-xs font-semibold uppercase tracking-wider text-indigo-600">Reflection prompt</p><h2 className="mt-1 text-base font-bold text-ink">Briefly answer the following about your LRU Cache implementation.</h2><p className="mt-2 text-sm text-slate-600">Keep your answer specific to the choices you made in Task 2.</p></div></div><ol className="mt-7 space-y-4">{prompts.map((prompt, index) => <li key={prompt} className="flex gap-3 text-sm leading-6 text-slate-700"><span className="flex h-6 w-6 shrink-0 items-center justify-center rounded-full bg-slate-100 text-xs font-bold text-slate-600">{index + 1}</span><span>{prompt}</span></li>)}</ol><form onSubmit={submit} className="mt-7 border-t border-slate-100 pt-6"><label htmlFor="answer" className="flex items-center justify-between text-sm font-semibold text-slate-700"><span>Your answer <span className="text-rose-500">*</span></span><span className={`text-xs font-normal ${answer.length > 3000 ? 'text-rose-600' : 'text-slate-400'}`}>{answer.length} / 3000</span></label><textarea id="answer" maxLength={3000} value={answer} onChange={e => { setAnswer(e.target.value); setError(''); setSubmitted(false); }} placeholder="Describe your data structures, complexity, trade-offs, and what you learned..." className={`mt-2 min-h-[240px] w-full resize-y rounded-xl border bg-white px-4 py-3 text-sm leading-6 text-slate-800 outline-none transition placeholder:text-slate-400 focus:border-brand focus:ring-4 focus:ring-indigo-100 ${error ? 'border-rose-400' : 'border-slate-300'}`} />{error && <p className="mt-2 flex items-center gap-1 text-sm text-rose-600"><CircleAlert size={14} />{error}</p>}{submitted && <p className="mt-4 flex items-center gap-2 rounded-lg bg-emerald-50 px-3 py-2.5 text-sm font-medium text-emerald-700"><Check size={15} />Your explanation has been saved for review.</p>}<button type="submit" className="primary mt-5 w-full">{submitted ? 'Update answer' : 'Mark task complete'} <Send size={16} /></button></form></section></div></main>; }
createRoot(document.getElementById('root')).render(<App />);
