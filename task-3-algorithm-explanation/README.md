# Task 3 — Algorithm Explanation

Standalone React + Vite + Tailwind reflection form for the LRU cache implementation.

```bash
npm install
npm run dev
```

Build with `npm run build` and preview with `npm run preview`.
