# Task 2 — LRU Cache

Standalone React + Vite + Tailwind implementation of an LRU cache. The implementation lives in `src/lruCache.js` and the UI demonstrates `get`, `put`, recency ordering, misses, and eviction.

```bash
npm install
npm run dev
```
