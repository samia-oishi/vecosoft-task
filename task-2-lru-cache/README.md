# Task 2 — LRU Cache

Standalone React + Vite + Tailwind cache playground with a client-side submission form.

```bash
npm install
npm run dev
```

Build with `npm run build` and preview with `npm run preview`.
